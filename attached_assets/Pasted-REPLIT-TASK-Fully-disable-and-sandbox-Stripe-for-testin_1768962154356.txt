REPLIT TASK: Fully disable and sandbox Stripe for testing (no billing, no redirects, no errors)

GOAL:
Temporarily REMOVE Stripe from the app runtime so:
- No Stripe API calls run
- No Stripe errors appear in logs
- No billing-related redirects or failures affect navigation
- App behaves as if all users are allowed (testing mode)
- Stripe can be safely re-enabled later

IMPORTANT:
This is TEMPORARY for testing. Do NOT delete code permanently — gate it.

────────────────────────
STEP 1 — DISABLE STRIPE INITIALIZATION
────────────────────────

In server/stripeClient.ts:

Wrap Stripe initialization in a feature flag:

const STRIPE_ENABLED = process.env.STRIPE_ENABLED === "true";

If STRIPE_ENABLED is false:
- Do NOT create Stripe client
- Export `null` or a stub instead

Example behavior:
- stripeClient should be undefined/null
- Any code that tries to use Stripe must short-circuit safely

────────────────────────
STEP 2 — SHORT-CIRCUIT BILLING ROUTES
────────────────────────

In server/billing.ts and server/routes.ts:

For ALL Stripe-related endpoints, add an early return:

If STRIPE_ENABLED === false:
- Return HTTP 503 with message:
  "Billing temporarily disabled for testing"

Affected endpoints include:
- POST /api/billing/checkout
- POST /api/billing/portal
- POST /api/billing/pack-checkout
- /api/stripe/webhook (should no-op safely)

DO NOT let these routes attempt Stripe calls.

────────────────────────
STEP 3 — DISABLE WEBHOOK HANDLER
────────────────────────

In server/webhookHandlers.ts:

If STRIPE_ENABLED === false:
- Immediately return 200 OK
- Log: "[Stripe] Webhook ignored — Stripe disabled"

This prevents Stripe auth errors entirely.

────────────────────────
STEP 4 — DISABLE PAYWALL & QUOTA ENFORCEMENT (TEST MODE)
────────────────────────

In server/middleware/paywall.ts and server/middleware/quota.ts:

Add a top-level bypass:

If process.env.STRIPE_ENABLED === "false":
- Allow ALL requests to pass
- Do NOT enforce:
  - Case limits
  - OCR quotas
  - AI quotas
  - Document export limits

This ensures testing is not blocked by billing logic.

────────────────────────
STEP 5 — FRONTEND: HIDE ALL BILLING UI
────────────────────────

In frontend:
- Plans page
- Account Settings → Subscription section
- Upgrade buttons
- Processing pack modals

Conditionally hide or disable when:
  STRIPE_ENABLED === false

Show a small note instead:
  "Billing temporarily disabled during testing"

DO NOT navigate users to checkout.

────────────────────────
STEP 6 — ENVIRONMENT SETUP
────────────────────────

In Replit Secrets:
Add or update:

STRIPE_ENABLED = false

(Optional but recommended)
Remove or blank:
- STRIPE_SECRET_KEY
- STRIPE_WEBHOOK_SECRET
- STRIPE_PUBLISHABLE_KEY

This guarantees Stripe can’t initialize accidentally.

────────────────────────
STEP 7 — VERIFY
────────────────────────

After changes:
- Restart app
- Navigate entire app
- Confirm:
  ✓ No Stripe errors in logs
  ✓ No redirects caused by billing
  ✓ All modules stay on their correct pages
  ✓ Case creation works
  ✓ AI flows work

────────────────────────
STEP 8 — DOCUMENT FOR LATER
────────────────────────

Add a comment in replit.md:

"Stripe is intentionally disabled via STRIPE_ENABLED=false during QA testing.
Re-enable only after final end-to-end verification."

END TASK