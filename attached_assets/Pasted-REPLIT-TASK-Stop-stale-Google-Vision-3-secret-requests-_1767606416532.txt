REPLIT TASK: Stop stale Google Vision “3 secret” requests and normalize to 1 API key

GOAL
The codebase must ONLY use GOOGLE_CLOUD_VISION_API_KEY.
Replit is still showing a stale “requested secrets” modal for:
- GOOGLE_CLOUD_PROJECT_ID
- GOOGLE_CLOUD_VISION_CLIENT_EMAIL
- GOOGLE_CLOUD_VISION_PRIVATE_KEY

PART 1 — VERIFY REPO HAS ZERO REFERENCES (must be 0)
1) Run these commands and paste the results:

rg -n "GOOGLE_CLOUD_PROJECT_ID|GOOGLE_CLOUD_VISION_CLIENT_EMAIL|GOOGLE_CLOUD_VISION_PRIVATE_KEY" .
rg -n "google-cloud/vision|@google-cloud/vision|VisionClient|private_key|client_email|project_id" server client shared .
rg -n "GOOGLE_API_KEY" server/services/evidenceExtraction.ts

If ANY matches exist, remove them and re-run the rg checks until all are 0.

PART 2 — FORCE SINGLE-SECRET CONFIG IN CODE (if not already)
In server/services/evidenceExtraction.ts:
- isVisionConfigured() must ONLY check process.env.GOOGLE_CLOUD_VISION_API_KEY
- remove any fallback to GOOGLE_API_KEY
In server/index.ts:
- startup log must be exactly: “Vision OCR configured: true/false (auth=api-key)”
In server/routes.ts:
- GET /api/vision/health must:
  - if missing key: return { ok:false, error:"missing GOOGLE_CLOUD_VISION_API_KEY" }
  - if present: make a tiny images:annotate call and return { ok:true } on success
In client/src/pages/AppEvidence.tsx:
- if OCR fails with missing key or 401/403:
  show: “OCR is not configured yet. Add GOOGLE_CLOUD_VISION_API_KEY in Replit Secrets.”

PART 3 — FIX THE REPLIT “STALE REQUESTED SECRETS” MODAL (UI STATE)
This modal is NOT coming from runtime code. It’s a pending/previous Agent “secret request.”
Do these actions:

A) Open Tools/Secrets in Replit.
- If these secrets exist but are blank, DELETE them:
  GOOGLE_CLOUD_PROJECT_ID
  GOOGLE_CLOUD_VISION_CLIENT_EMAIL
  GOOGLE_CLOUD_VISION_PRIVATE_KEY
- Ensure ONLY this exists:
  GOOGLE_CLOUD_VISION_API_KEY = <your key>

B) Clear the pending secret request:
- In the Agent panel, find the previous “1 secret requested / 3 secrets requested” request card.
- Click “Dismiss/Cancel” (or the “x”) to cancel the pending request.
- If there is a “Continue” button attached to that request, DO NOT click Continue; dismiss it instead.

C) Hard refresh:
- Stop the running workflow
- Restart the workflow
- Hard refresh the browser tab (Cmd+Shift+R / Ctrl+Shift+R)

PART 4 — REPORT BACK (REQUIRED)
After changes, report:
1) Files changed
2) rg count results for the 3 old secret names (must be 0)
3) Confirm only required secret is GOOGLE_CLOUD_VISION_API_KEY
4) Show /api/vision/health result:
   - missing key -> ok:false with missing-key error
   - present key -> ok:true
5) Confirm an image upload successfully OCRs with provider metadata